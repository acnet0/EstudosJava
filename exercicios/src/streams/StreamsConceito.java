package streams;

public class StreamsConceito {

	/*
	 * - Built Ops
	 * Forma como criamos os Strams, utilizando metodos estaticos, listas, etc
	 * - Intermedian. Ops
	 * Auxliam a transformar os dados, são as funções, o encadeamento de funções no tratamento dos dados
	 * - Terminal Ops 
	 * Todas as operações intermediarias retornam uma Stream, e no fim desta sequencia temos a operação final, que pode retornar algo diferente de um Stream 
	 * 
	 * Built -> Int -> Int -> Int -> Int -> ... -> Int -> Ter
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	*/

}
